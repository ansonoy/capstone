
/**import React from 'react';
import {GoogleMap, LoadScript, Marker} from "@react-google-maps/api"

const Map: React.FC = () => {
    const mapStyles: any = {
        height: '400px',
        width: '100%',
    };
  
    const defaultCenter = {
        lat: 0, // Replace with your desired initial latitude
        lng: 0, // Replace with your desired initial longitude
    };
  
    return (
        <LoadScript
            googleMapsApiKey="AIzaSyBYEeWyX2jqtqpSe1AGEUwal1f8dS00zjw"
        >

            <GoogleMap
                mapContainerStyle={mapStyles}
                center={defaultCenter}
                zoom={8}
            >
                <Marker position={defaultCenter} />
            </GoogleMap>
        </LoadScript>
    );
};

export default Map; */

